{
  "name": "web",
  "version": "1.0.0",
  "scripts": {
    "serve": "esbuild --bundle --target=es2020 --sourcemap --servedir=. --outfile=bundle.js main.ts"
  },
  "devDependencies": {
    "esbuild": "^0.15.14",
    "typescript": "^4.9.3"
  }
}
